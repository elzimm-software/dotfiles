[Login]
# mod+slash now locks the screen (see ~/.config/sway/config.tmpl and
# ~/.config/scroll/config.tmpl) instead of suspending, so give the physical
# power button the suspend duty instead of its systemd default (poweroff).
# Universal -- not tied to any particular WM.
HandlePowerKey=suspend
