[Login]
# mod+slash now locks the screen (see i3/config) instead of suspending, so
# give the physical power button the suspend duty instead of its systemd
# default (poweroff).
HandlePowerKey=suspend
