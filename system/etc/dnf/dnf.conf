# see `man dnf.conf` for defaults and possible options

[main]
# Make Enter default to "yes" on confirmation prompts, like pacman's
# [Y/n]. Not the same as assumeyes, which skips prompting entirely.
defaultyes=True
